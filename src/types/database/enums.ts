export enum Tables {
    // osu! tables
    SCORES = "scores",
    SCORES_PP = "scores_pp",

    // Discord tables
    USERS = "users",
    SERVERS = "servers",
    COMMANDS = "commands",
    SLASH_COMMANDS = "slash_commands",
}
